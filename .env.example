# Copy to .env and fill in. .env is gitignored.

# Experiment tracking (optional — falls back to local CSV/TensorBoard if unset)
WANDB_API_KEY=
WANDB_PROJECT=wildlife-classifier
WANDB_ENTITY=

# Serving (Phase 9+)
WILDLIFE_MODEL_DIR=outputs/serving
WILDLIFE_ALLOWED_ORIGINS=http://localhost:5173
