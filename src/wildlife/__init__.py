"""wildlife — taxonomy-neutral fine-grained species classifier.

The package is deliberately named ``wildlife`` (not ``birds``): birds are the only
taxon populated in this build, but the core code assumes nothing bird-specific so
mammals/other taxa can be added later via config + a specialist head, not a rewrite.
"""

__version__ = "0.1.0"
