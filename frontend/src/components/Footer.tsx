export function Footer() {
  return (
    <footer className="mx-auto w-full max-w-3xl px-5 py-8 text-center text-xs text-muted">
      <p>
        Fine-grained classifier trained on NABirds (555 species). Predictions are model
        estimates, not expert identifications.
      </p>
    </footer>
  );
}
